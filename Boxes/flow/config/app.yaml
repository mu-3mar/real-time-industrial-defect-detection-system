# Optional CORS configuration.
# Leave empty or omit to allow all origins (works for localhost, LAN, remote).
# To restrict origins (e.g. production), list them:
#   cors_origins:
#     - "http://localhost:3000"
#     - "https://myapp.example.com"
cors_origins: []
