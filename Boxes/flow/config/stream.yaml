# Capture resolution (V4L2 + MJPG). FPS is not set by the app — camera default.
# Source device: POST /api/reports/open (camera_source).
width: 1920
height: 1080

# Adaptive detection throttle (frames between runs)
box_detect_every_n_frames: 1
defect_detect_every_n_frames: 1

# ROI gate (two vertical lines + one horizontal line)
# Increasing `roi_width` moves the vertical lines further apart (wider gate).
# Changing `roi_center_offset` shifts the vertical gate left/right (relative to the info panel).
# `roi_top_y` sets the horizontal line position (y-coordinate, top of ROI)
roi_width: 1000
roi_center_offset: 950
roi_top_y: 200

# Strict per-frame debug processing.
# Forces both detectors to run every frame and consumes queued frames in order.
strict_debug_mode: false
strict_crop_padding_ratio: 0.08
strict_frame_queue_size: 120

# Strict current-frame validation mode.
strict_current_frame_mode: false
