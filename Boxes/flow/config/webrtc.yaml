# WebRTC ICE configuration. Single source of truth for STUN/TURN.
# Used by backend (api_server) and exposed to frontend via GET /api/config.
stun:
  urls: "stun:20.51.117.96:3478"

turn:
  urls: "turn:20.51.117.96:3478"
  username: "turnuser"
  credential: "Sup3r$tr0ngP@ssw0rd"

# Set to true only for testing TURN relay (forces iceTransportPolicy: "relay").
# Normal operation: keep false so WebRTC uses direct/srflx first, TURN only as fallback.
debug_turn_only: false
