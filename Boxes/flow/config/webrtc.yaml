# WebRTC ICE configuration. Backend uses full config for connections.
# GET /api/config returns only STUN (no TURN credentials) to clients.
stun:
  urls: "stun:20.51.117.96:3478"

turn:
  urls: "turn:20.51.117.96:3478"
  secret: "Kf83Jd9sL2PqX91AbC77LmN"

# debug_turn_only: Not implemented (aiortc has no iceTransportPolicy). Omit or false.
# Forcing WebRTC connection over Relay (TURN) only.
force_relay: true

