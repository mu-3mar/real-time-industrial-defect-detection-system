# `Boxes/flow/detectors`

Detector wrappers used by the flow pipeline.

## Key files

- `detector.py`: thin wrapper around an Ultralytics YOLO model instance with consistent thresholds/device handling.
